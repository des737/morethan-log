type Props = {
  id: string
  slug: string
  title: string
}

const Cusdis: React.FC<Props> = ({ id, slug, title }) => {
  // Cusdis is disabled in site.config.js
  // Install react-cusdis if you want to enable it
  return null
}

export default Cusdis
